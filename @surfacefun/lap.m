function L = lap(f)
%LAP   Laplacian of a SURFACEFUN.
%   LAP(F) returns a SURFACEFUN representing the Laplacian of F.
%
%   This is shorthand for LAPLACIAN(F).
%
%   See also LAPLACIAN.

L = laplacian(f);

end
